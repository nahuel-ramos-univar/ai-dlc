# Triage boundaries

A defect fix restores approved or documented behavior. Route additions, changed business rules, broad redesigns, or unclear desired behavior to `plan-work`.

One defect should produce one canonical Bug record when an external record is authorized. Link corroborating reports instead of duplicating them. A diagnosis that cannot reproduce the issue must state the missing evidence and avoid claiming a root cause.
